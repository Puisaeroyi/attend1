# Core dependencies
openpyxl>=3.1.0
pandas>=2.0.0
pyyaml>=6.0
xlsxwriter>=3.0.0

# Testing
pytest>=7.4.0
pytest-cov

# GUI packaging (Windows only)
# pyinstaller>=6.0.0  # Uncomment when building on Windows
